# Vendored for CineViet app build. Keep this local options file minimal so
# root `flutter analyze` does not require mobile_scanner's upstream dev-only
# analysis package.
include: package:flutter_lints/flutter.yaml
